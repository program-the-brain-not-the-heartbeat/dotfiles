# ~/.curlrc

# Fail on HTTP errors (4xx/5xx) so scripts can detect problems
--fail

# Show a nice progress bar in interactive use, but be quiet otherwise
--silent
--show-error

# Stronger protocol restrictions
--proto =https
--proto-redir =https
--tlsv1.2

# Reasonable timeouts
--connect-timeout 10
--max-time 0

# Follow redirects
--location

# HTTPS verification ON (don't turn this off globally)
--ssl-reqd

# Use .netrc for auth when requested with -n
# (Create ~/.netrc if needed)
# Example:
# machine example.com login myuser password mypass
# (and chmod 600 ~/.netrc)
